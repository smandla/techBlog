<section class="card">
  <a href="/blog/{{id}}">
    <header><span>
      {{post_title}}</span>
      <span>Posted by 
      {{user.username}} on {{format_date createdAt}}</span></header>
    {{!-- <img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}"> --}}
    <div class="card-body">
      {{content}}
    </div>
  </a>
</section>